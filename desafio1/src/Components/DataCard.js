/* eslint-disable react/jsx-one-expression-per-line */
import React from 'react';

const DataCard = ({ title }) => (
  <h4 className="Card">{title}</h4>
);

export default DataCard;

//  {score && <p className="Card-content">Score: {score}</p> }
